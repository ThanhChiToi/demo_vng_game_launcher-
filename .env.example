# Địa chỉ backend .NET. Copy file này thành .env rồi chỉnh nếu đổi port.
VITE_API_URL=https://localhost:7123/api

# Đặt true để đăng nhập giả lập (gõ tài khoản/mật khẩu gì cũng vào được).
# Dùng khi code UI mà chưa có backend .NET. NHỚ TẮT khi nối API thật.
VITE_USE_MOCK_AUTH=false
